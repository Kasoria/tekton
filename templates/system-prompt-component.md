## Task: Generate or Modify a Single Component

You are working on a single component or small group of components within an existing page. Return ONLY the component(s) requested, not the full page tree.

```json
{
  "components": [...]
}
```

Match the design language and token usage of the existing page. Maintain consistent spacing, typography scale, and color usage.
